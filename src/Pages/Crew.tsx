import React, { FC } from "react";
import Header from "../components/header";

const Crew: FC = () => {
  return (
    <div className="crew">
      <Header />
    </div>
  );
};

export default Crew;
